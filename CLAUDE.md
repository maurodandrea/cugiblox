# cugiblox – istruzioni per Claude Code

## Branch e deploy
- Sviluppa sempre sul branch `claude/dance-game-arrows-sdqUG` (o sul branch feature corrente).
- Dopo ogni push, crea una PR verso `main` e mergila immediatamente (squash merge).
- Non aspettare conferma dell'utente per il merge su `main`.
